trigger:
- '*'

pr: 
- '*'

pool: Default

resources:
  repositories:
    - repository: Template
      type: github
      name: gunnargestsson/bcbuildtemplate
      ref: master

variables:
- group: BuildVariables

extends:
  template: /.azureDevOps/CI.yml@Template
  parameters:
    version: "current"
    appVersion: "17.2"

